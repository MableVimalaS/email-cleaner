name: email_cleaner
description: "Email cleanup app - scan, categorize, and bulk delete emails"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  enough_mail: ^2.1.0
  provider: ^6.1.0
  shared_preferences: ^2.2.0
  flutter_secure_storage: ^9.0.0
  url_launcher: ^6.2.0
  intl: ^0.19.0
  flutter_slidable: ^3.0.0
  uuid: ^4.2.0
  collection: ^1.18.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
